rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // GEÇİCİ - TÜM ERİŞİMLERE İZİN VER (TEST İÇİN)
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
